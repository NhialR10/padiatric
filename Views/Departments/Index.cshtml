@model IEnumerable<Padiatric.Models.Department>

@{
    ViewData["Title"] = "Departments";
}

<div class="container my-5">
    <h2 class="text-center mb-4">Departments</h2>

    <div class="table-responsive">
        <table class="table table-bordered table-hover">
            <thead class="table-primary">
                <tr class="text-center">
                    <th>@Html.DisplayNameFor(model => model.Name)</th>
                    <th>@Html.DisplayNameFor(model => model.HastaSayisi)</th>
                    <th>@Html.DisplayNameFor(model => model.BosyatakSayisi)</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr class="text-center">
                        <td>@Html.DisplayFor(modelItem => item.Name)</td>
                        <td>@Html.DisplayFor(modelItem => item.HastaSayisi)</td>
                        <td>@Html.DisplayFor(modelItem => item.BosyatakSayisi)</td>
                        <td>
                            <a class="btn btn-primary btn-sm me-1" asp-action="Edit" asp-route-id="@item.Id">
                                <i class="bi bi-pencil-square"></i> Edit
                            </a>
                            <a class="btn btn-info btn-sm me-1 text-white" asp-action="Details" asp-route-id="@item.Id">
                                <i class="bi bi-info-circle"></i> Details
                            </a>
                            <a class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@item.Id">
                                <i class="bi bi-trash"></i> Delete
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <div class="text-center mt-4">
        <a class="btn btn-success btn-lg" asp-action="Create">
            <i class="bi bi-plus-circle"></i> Create Department
        </a>
    </div>
</div>
